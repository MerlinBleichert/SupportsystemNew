<UserControl x:Class="Supportsystem.OptionPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Supportsystem"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      >

    <Border Background="#bbbbbb">
        <Grid>
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                <StackPanel
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    TextBlock.TextAlignment="Center">

                    <Button Content="Maschinenkatalog" Command="{Binding CataloguePageCommand}" Width="200" Margin="0,20"/>
                    <Button Content="Offene Störungen" Command="{Binding FaultOverviewPageCommand}" Width="200"/>
                    <Button Content="Admin" Width="200" Margin="0,20"/>

                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>
